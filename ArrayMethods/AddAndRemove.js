let fruits = ["apple", "banana", "cherry"];

fruits.push("orange");  //adds at the end of the array
console.log(fruits);
fruits.pop(); //removes at the end of the array
console.log(fruits);
fruits.shift(); //removes at the beginning of the array
console.log(fruits);
fruits.unshift("apple"); //adds at the beginning of the array
console.log(fruits);